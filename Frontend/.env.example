# ============================================
# Craftique Frontend Environment Configuration
# ============================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control

# ============================================
# API Configuration
# ============================================
# Backend API base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# ============================================
# Auth0 Configuration
# ============================================
# Get these from your Auth0 Dashboard: https://manage.auth0.com/
#
# Create an Auth0 application with:
# - Type: Regular Web Application
# - Allowed Callback URLs: http://localhost:3000/auth/callback
# - Allowed Logout URLs: http://localhost:3000
#
# Generate AUTH0_SECRET using:
#   openssl rand -hex 32
#
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_SECRET=example
APP_BASE_URL=http://localhost:3000

# Optional: Auth0 audience for custom APIs
# AUTH0_AUDIENCE=https://your-api-identifier

# ============================================
# Notes
# ============================================
# 1. All NEXT_PUBLIC_* variables are exposed to browser
# 2. Never put secrets in NEXT_PUBLIC_* variables
# 3. For production, update APP_BASE_URL to your production URL
# 4. The SDK automatically provides these routes:
#    - /auth/login (login)
#    - /auth/logout (logout)
#    - /auth/callback (callback - add to Allowed Callback URLs)
#    - /auth/profile (session info)
#    - /auth/access-token (access token)
#    - /auth/backchannel-logout (backchannel logout)
